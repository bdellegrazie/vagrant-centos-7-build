---
# Python libraries for Ansible
- name: Python libraries for Ansible
  package:
    name:
      - libselinux-python
      - libsemanage-python
    state: present
  when: ansible_os_family == 'RedHat'
